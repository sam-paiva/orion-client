export class Address {
  city: string;
  district: string;
  state: string;

  constructor(city: string, district: string, state: string) {
    this.city = city;
    this.district = district;
    this.state = state;
  }
}
